{
  "type": "template",
  "name": "03-realloc",
  "title": "realloc 重新配置（範本）",
  "description": "使用 realloc() 調整記憶體大小範本",
  "topic": "c/09-memory",
  "language": "c",
  "code": "#include <stdlib.h>\n\n{{型別}} *{{指標}} = ({{型別}}*)malloc({{原始大小}} * sizeof({{型別}}));\n\n// 重新調整大小\n{{型別}} *{{新指標}} = ({{型別}}*)realloc({{指標}}, {{新大小}} * sizeof({{型別}}));\nif ({{新指標}} == NULL) {\n    printf(\"重新配置失敗\\n\");\n    free({{指標}});\n    return 1;\n}\n{{指標}} = {{新指標}};\n\nfree({{指標}});"
}