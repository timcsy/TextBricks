{
  "type": "template",
  "name": "07-array-sort",
  "title": "陣列排序（範本）",
  "description": "使用氣泡排序法排序陣列範本",
  "topic": "c/04-arrays",
  "language": "c",
  "code": "{{元素型別}} {{陣列名稱}}[{{大小}}];\n\n// 氣泡排序\nfor (int {{外層索引}} = 0; {{外層索引}} < {{大小}} - 1; {{外層索引}}++) {\n    for (int {{內層索引}} = 0; {{內層索引}} < {{大小}} - {{外層索引}} - 1; {{內層索引}}++) {\n        if ({{陣列名稱}}[{{內層索引}}] > {{陣列名稱}}[{{內層索引}} + 1]) {\n            {{元素型別}} {{暫存}} = {{陣列名稱}}[{{內層索引}}];\n            {{陣列名稱}}[{{內層索引}}] = {{陣列名稱}}[{{內層索引}} + 1];\n            {{陣列名稱}}[{{內層索引}} + 1] = {{暫存}};\n        }\n    }\n}"
}